# Multivariate Survival and Claim Frequency Modelling with Phase-Type Distributions

Bivariate modelling of (claim frequency, claim severity) on the real
**French Motor Third-Party Liability Claims** dataset (freMTPL2, 678,013
policies), using MVN-approximated copula structure, MANOVA, QDA, and
MAP/PH-inspired feature engineering, feeding a Gradient Boosting classifier
that flags high-risk policies pre-renewal — served via a risk-scoring API.

## Why this project exists

Traditional actuarial pricing treats claim frequency and severity as
independent univariate processes (a separate frequency GLM x a separate
severity GLM). This project tests that assumption directly: it fits a
bivariate normal to (log-frequency, log-severity), tests whether the
correlation is really zero, tests whether claim *behaviour* differs across
policyholder segments (MANOVA), classifies policies into risk tiers from
the joint distribution (QDA), engineers phase-type severity descriptors
per rating cell, and trains a classifier to flag policies likely to
produce a large loss before their next renewal.

## Results at a glance (real freMTPL2 data, n = 678,013 policies)

| Question | Method | Result |
|---|---|---|
| Are frequency & severity independent? | Bivariate MVN fit + correlation | **rho = +0.106** (mildly, not strongly, dependent — see REPORT.md for discussion of direction) |
| Are claimant profiles multivariate-normal? | Mahalanobis D^2 vs chi2(2), KS test | Rejected (p < 0.001) — real claims are heavier-tailed than MVN; 3.6% of claimants flagged as outliers at alpha=0.01 |
| Does claim behaviour differ by segment? | One-way MANOVA (Wilks' Lambda) | **Wilks' Lambda = 0.972, p < 0.001** — segments differ significantly |
| Can we classify risk tier from (freq, sev)? | QDA | **84.3% test accuracy** across Low/Medium/High tertiles |
| Can we flag large-loss policies pre-renewal? | Gradient Boosting + PH features | **AUC = 0.712**, PR-AUC = 0.068 (2% base rate) |

Full methodology, equations, and discussion in **REPORT.md**. All numbers
above are regenerated by `scripts/run_pipeline.py` and written to
`outputs/results/results.json` — nothing here is hand-typed.

## Project structure

```
data/
  generate_data.py     synthetic-data fallback generator (used only if no real data present)
  raw/                 freMTPL2freq.csv + freMTPL2sev.csv (bundled)
frontend/
  index.html            the site (React via CDN, zero build step — see "Frontend" below)
src/
  data_processing.py   loading (real + synthetic), cleaning, feature engineering
  mvn_analysis.py       bivariate MVN fit, conditional distribution, Mahalanobis/chi2
  manova_analysis.py    one-way MANOVA from first principles (Wilks' Lambda, Pillai's trace)
  ph_features.py         MAP/PH-inspired moment-matched phase-type features
  qda_classifier.py      QDA risk-tier classifier
  ml_classifier.py       Gradient Boosting high-risk classifier
  plotting.py             all figure generation
app/
  main.py                production FastAPI service — serves the frontend at "/" AND the JSON API
  scoring.py               shared scoring logic (used by both servers below)
  demo_server.py           dependency-free stdlib HTTP server (see "Why two servers" below)
  schemas.py               pydantic request/response models
scripts/
  run_pipeline.py         orchestrates the full analysis end-to-end
tests/
  test_pipeline.py         25 assertion-based tests validating the stats machinery
outputs/
  plots/                  all generated figures (also served at /outputs/plots/*.png)
  artifacts/               trained model + MVN/PH parameters
  results/                  results.json (every number in this README + the live frontend)
.github/workflows/
  tests.yml                CI: runs the test suite + a live API smoke test on every push
Dockerfile, .devcontainer/  deployment (see "Deployment")
```

## Setup

```bash
pip install -r requirements.txt

# Put the real data here (see "Getting the data" below), then:
python scripts/run_pipeline.py     # trains everything, writes outputs/

# Run the test suite (no pytest required, though it will also work under pytest):
python tests/test_pipeline.py
```

### Getting the data

The real dataset is already included in this package at:

```
data/raw/freMTPL2freq.csv
data/raw/freMTPL2sev.csv
```

so `python scripts/run_pipeline.py` will pick it up automatically — no
download needed. For reference, or if you need to refresh/replace these
files, the same schema is available from:
- Kaggle: `kaggle.com/datasets/karansarpal/fremtpl2-french-motor-tpl-insurance-claims`
- OpenML (no login needed): `sklearn.datasets.fetch_openml(data_id=41214)` (freq) and `data_id=41215` (sev)
- Hugging Face mirror: `huggingface.co/datasets/mabilton/fremtpl2`

`src/data_processing.py` auto-detects these files and applies the standard
cleaning steps used in the literature (Exposure clipped to 1 year, ClaimNb
clipped to 4, a known ClaimAmount==0-with-ClaimNb>=1 data artifact
corrected, ClaimAmount clipped at 200,000) before merging. **If the files
are absent, the pipeline automatically falls back to a synthetic dataset**
(`data/generate_data.py`) built with a known Gaussian-copula dependence
structure — useful for testing the pipeline or reproducing behaviour in an
offline environment, but the headline numbers above are from the real data.

## Running the site + API

Both servers below now serve **the same frontend** at `/` in addition to
the JSON API — there's nothing separate to build or deploy for the UI.

**Production (FastAPI)** — the real deliverable, meant for deployment:
```bash
uvicorn app.main:app --reload --port 8000
# -> http://localhost:8000/        the site (scorer + findings + evidence)
# -> http://localhost:8000/docs    interactive Swagger UI
# -> http://localhost:8000/api     plain JSON status
```

**Demo (stdlib only, no dependencies)** — see "Why two servers" below:
```bash
python app/demo_server.py 8000
# serves the identical frontend + API at the same paths as above
```

Calling the API directly still works exactly as before:
```bash
curl -X POST http://localhost:8000/score -H "Content-Type: application/json" -d '{
  "Area": "C", "VehPower": 7, "VehAge": 4, "DrivAge": 28, "BonusMalus": 95,
  "Density": 3200, "Region": "Rhone-Alpes", "VehGas": "Diesel",
  "VehBrand": "B4", "Exposure": 0.85
}'
# -> {"high_risk_probability": 0.76, "high_risk_flag": false, "risk_tier": "Low", ...}
```

`/score/batch` accepts `{"policies": [...]}` for multiple policies in one call.

## Frontend

`frontend/index.html` is a single self-contained page — React and Babel
loaded from cdnjs, JSX compiled in-browser, no `npm install` or build
step. That's a deliberate choice, not a shortcut: it means the exact same
file runs identically whether you open it locally, serve it from
Codespaces, or drop it into any static host, with zero build
infrastructure to keep working. It has three parts:

- **A live scorer in the hero.** The product's whole job is turning
  policy inputs into a number, so the interactive form *is* the opening
  moment, not a demo bolted onto a marketing page. Submitting calls
  `POST /score` on whichever origin the page is served from.
- **A findings ledger and evidence gallery**, populated by fetching
  `/outputs/results/results.json` and `/outputs/plots/*.png` at page
  load — so if you retrain the model, the page's own headline numbers
  update automatically instead of drifting out of sync with hand-typed copy.
- **An "API base" override** (footer) for the one case where frontend and
  backend end up on different origins (e.g. a statically-hosted frontend
  pointed at a separately-hosted API) — defaults to same-origin and can be
  ignored entirely when both are served together, which is the normal case.

### Why two servers?

`app/main.py` (FastAPI/uvicorn) is the intended production service — the
right tool for a real deployment (GitHub Codespaces, Docker, any ASGI
host). `app/demo_server.py` is a zero-dependency fallback built from the
Python standard library only, because the FastAPI/uvicorn packages could
not be installed in the offline sandbox this project was built in (no
network access to PyPI). It exercises the *exact same* scoring code path
(`app/scoring.py`) and serves the *exact same* frontend file, so it's a
faithful proof that the whole thing works end-to-end, not just written but
unverified. In any environment with internet access, prefer `app/main.py`.

## Deployment

**GitHub Codespaces** — this is what makes the site reachable from
somewhere other than your own machine, launched entirely from GitHub:
1. Push this repo to GitHub (data, trained model artifacts, and the
   frontend are all already committed — no separate build/training step
   is required before deploying).
2. Open the repo in a Codespace. `.devcontainer/devcontainer.json`
   installs dependencies, **automatically starts the API** in the
   background on port 8000, and marks that port **publicly visible** —
   so anyone with the forwarded URL can open the site, not just you
   while signed into the Codespace.
3. Codespaces will prompt "Open in Browser" for port 8000 — that URL
   (`https://<codespace-name>-8000.app.github.dev`) is the live, public
   site. Share that link directly; append `/docs` for the API explorer.
4. If the port doesn't show as Public automatically: **Ports tab → right-click
   port 8000 → Port Visibility → Public.**

**GitHub Actions (CI)** — `.github/workflows/tests.yml` runs the full
25-test statistical suite plus a live smoke test of the API on every push,
directly on GitHub's own infrastructure. This is a second, complementary
sense of "runs on GitHub": Codespaces makes the app reachable publicly;
Actions proves the code works on every commit without you running anything
locally first.

**Docker** (any other container platform — Render, Fly.io, Cloud Run, etc.):
```bash
docker build -t risk-scoring-api .
docker run -p 8000:8000 risk-scoring-api
```

**LinkedIn walkthrough video**: record a 3-5 minute screen capture
covering (1) the problem statement and why frequency/severity dependence
matters, (2) the live site's scorer + findings ledger, (3) the MVN
scatter+contour plot and the correlation finding, (4) a live `/score` call
against the public Codespace URL from your phone or another machine (to
prove it's really public, not just localhost), (5) the feature-importance
plot.

## Generated plots (`outputs/plots/`)

| File | Shows |
|---|---|
| `01_mvn_scatter_contour.png` | Claimant (freq, severity) scatter with fitted MVN density contours |
| `02_chi2_qq_plot.png` | Chi-square Q-Q plot: MVN goodness-of-fit diagnostic |
| `03_mahalanobis_outliers.png` | Policies flagged as multivariate outliers (alpha=0.01) |
| `04_manova_segment_means.png` | Segment mean claim behaviour + MANOVA test statistic |
| `05_qda_decision_boundary.png` | QDA decision regions for Low/Medium/High risk tiers |
| `06_roc_pr_curves.png` | ROC and Precision-Recall curves for the high-risk classifier |
| `07_feature_importance.png` | Top predictors of the high-risk flag |

## Known limitations (see REPORT.md for full discussion)

- Real MTPL data rejects exact bivariate normality (heavy tails) —
  motivating a true copula (not just MVN) as a natural next step.
- The 5-95% correlation direction (positive, not negative) differs from
  the naive "high-frequency -> low-severity" stylized fact often cited;
  this is a genuine empirical finding on this book of business, not an
  error — discussed in REPORT.md.
- PH-inspired features are fit per rating cell (pooled across policies in
  that cell, not per-individual-policy), since a single policy rarely has
  enough claims to fit a distribution to.
- The Gradient Boosting classifier does not enforce monotonicity
  constraints on rating factors (e.g. BonusMalus); a production system
  would typically add these for auditability.
